html结构,必须设置nano的告高度
 <div class="nano">
    <div class="nano-content">内容....</div>
  </div>

Jquery调用:
$(function()
{
$(".nano").nanoScroller({ scroll:'top'}); //top,bottom可选
});

必须：内容不能隐藏，否组不能出现滚动条，可以先通过先显示隐藏内容，然后调用nanoScroller后再影藏